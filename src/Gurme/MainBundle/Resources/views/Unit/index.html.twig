{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Unit list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Short</th>
                <th>Long</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('db_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.short }}</td>
                <td>{{ entity.long }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('db_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('db_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('db_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
